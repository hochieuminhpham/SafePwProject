spring.application.name=SafePasswordProjectMiph

# Database connection settings with direct environment property fallbacks
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/sip_tozzo?serverTimezone=Europe/Zurich}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Wvxscytheis=p2007}

# Hibernate / JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


